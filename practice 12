#include <stdio.h>
#define MAX 5
int s[MAX],top=-1;
void push(int x){if(top<MAX-1)s[++top]=x;}
int pop(){return top>=0?s[top--]:-1;}
int peek(){return top>=0?s[top]:-1;}
int main(){
 push(10);push(20);printf("%d ",pop());printf("%d",peek());
}
