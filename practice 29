#include <stdio.h>
int main(){
    int a[10],n,c;scanf("%d",&n);
    for(int i=0;i<n;i++)scanf("%d",&a[i]);
    for(int i=0;i<n;i++){
        c=1;
        for(int j=i+1;j<n;j++)if(a[i]==a[j]){c++;a[j]=-1;}
        if(a[i]!=-1)printf("%d:%d\n",a[i],c);
    }
}
