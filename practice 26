#include <stdio.h>
int main(){
    int a[10][10],r,c,max;scanf("%d%d",&r,&c);
    for(int i=0;i<r;i++)for(int j=0;j<c;j++)scanf("%d",&a[i][j]);
    max=a[0][0];
    for(int i=0;i<r;i++)for(int j=0;j<c;j++)if(a[i][j]>max)max=a[i][j];
    printf("Largest:%d",max);
}
