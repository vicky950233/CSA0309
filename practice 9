#include <stdio.h>
int main() {
    int n, a = 0, b = 1, c, sum = 1;
    scanf("%d", &n);
    if (n <= 1) { printf("%d", n); return 0; }
    for (int i = 2; i < n; i++) { c = a + b; sum += c; a = b; b = c; }
    printf("%d", sum);
}
